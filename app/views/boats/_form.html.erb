<%= form_with(model: boat, local: true) do |form| %>
  <% if boat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boat.errors.count, "error") %> prohibited this boat from being saved:</h2>

      <ul>
      <% boat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :boat_name %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.number_field :size, id: :boat_size %>
  </div>

  <div class="field">
    <%= form.label :max_load %>
    <%= form.number_field :max_load, id: :boat_max_load %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :boat_image_url %>
  </div>

  <div class="field">
    <%= form.label :assign_jobs %>
    <%= form.number_field :assign_jobs, id: :boat_assign_jobs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
